#Geting started Node.js server

Install all the required dependencies

### `npm install`

Install Nodemon, if you're not already install it on your npm

### `npm install -g nodemon`

Run the server

### `nodemon src/index`
